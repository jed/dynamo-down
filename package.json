{
  "name": "dynamodown",
  "version": "1.0.0",
  "description": "A leveldown API implementation on AWS DynamoDB",
  "author": "Jed Schmidt <where@jed.is>",
  "license": "MIT",
  "main": "lib/dynamodown.js",
  "scripts": {
    "build": "babel src --out-dir lib",
    "test": "npm run build && node lib/test.js",
    "prepublish": "npm run build"
  },
  "devDependencies": {
    "async": "^0.9.0",
    "aws-sdk": "^2.1.10",
    "babel": "^4.0.1",
    "concat-stream": "^1.4.7",
    "dynalite": "^0.5.2",
    "levelup": "^0.19.0"
  },
  "dependencies": {
    "abstract-leveldown": "^2.1.0",
    "xtend": "^4.0.0"
  }
}
